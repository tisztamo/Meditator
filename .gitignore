package-lock.json
node_modules/

node_modules/
data/
log/
audit.db

# runtime artifacts of a living mind
interrupt-state/
state/
journal/
*.log
.run/

# prompt dumps from MEDITATOR_DEBUG_PROMPTS (debug only; can grow large)
debug/

# the memory vault is its own git repository (see COVENANT.md)
memory/

# Studio deployment secrets (see doc/serving-remotely.md)
.env.studio
.env

# local list of external projects the Studio tends (machine-specific abs paths)
config/studio-projects.json
